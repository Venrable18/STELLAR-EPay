# Circuits

This directory will hold Circom circuits for the MVP:

- `deposit/`
- `transfer/`
- `withdraw/`
- `lib/` shared gadgets (Poseidon, Merkle, and so on)

Build outputs should go under `circuits/build/` (ignored by git).
